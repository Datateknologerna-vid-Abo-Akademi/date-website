services:
  db:
    restart: always
    ports: !reset []

  web:
    image: ghcr.io/datateknologerna-vid-abo-akademi/date-website:${IMG_TAG}
    restart: always
    command: /bin/bash ./web.prod.sh
    ports: !reset []

  asgi:
    image: ghcr.io/datateknologerna-vid-abo-akademi/date-website:${IMG_TAG}
    restart: always
    command: /bin/bash -c "daphne -b 0.0.0.0 core.routing:application"
    env_file:
      - ${ENV_FILE}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  celery:
    restart: always
    image: ghcr.io/datateknologerna-vid-abo-akademi/date-website:${IMG_TAG}
    env_file:
      - ${ENV_FILE}

  redis:
    restart: always
    ports: !reset []
    
  nginx:
    restart: always
    image: nginx:stable-alpine
    ports:
      - ${DJANGO_PORT}:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web
      - asgi
